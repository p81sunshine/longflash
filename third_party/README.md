This directory is reserved for optional external checkouts that are not bundled
with the artifact.

Expected use:

- `third_party/EAGLE/` for reproducing the EAGLE-3 baseline scripts.

The main LongFlash/DFlash artifact does not import code from this directory.
